#include <stdio.h>
int main(){
    int a = 5;
    if(a > 0){            // Check if a is positive "if" and bracket says(i am brackeet i will only bother about conditions) after the condition to print some thing we use { in side this printf function exists}
        printf("Positive\n");
    } 
    return 0;
}