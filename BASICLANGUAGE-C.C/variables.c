#include <stdio.h>
 int main (){
    int a=5;//don't bother about what is int i will explain you by datatype topic.
    //here "a" is variable  (like tiffin box -a and 5 is delicious food in that box)if hope this analogy helps you
     printf("%d",a);//here also %d is used to print integer value where "a" is variable(store that number in that box)%d always should be in columns ("") like printf("content" )
 return 0;
 }